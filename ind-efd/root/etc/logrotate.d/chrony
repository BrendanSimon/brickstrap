#!
#! SEPL:
#!
#! limit size to 1MB, 7 rotations.
#! NOTE: time interval is ignored when using `size`.
#!

/var/log/chrony/*.log {
	size 1M
	rotate 7
	missingok
	notifempty
	compress
	delaycompress
	sharedscripts
	postrotate
		/usr/bin/chronyc -a cyclelogs > /dev/null 2>&1 || true
	endscript
}
